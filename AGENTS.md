# Repository Notes

- After app UI or behavior changes, run `swift build`, `swift test`, and `swift run jocalhost-checks`, then deploy the current build to the MacBook with `./scripts/deploy-remote-mac.sh macbook-pro-4.local` so it can be reviewed there.
- Leave unrelated user changes untouched.
